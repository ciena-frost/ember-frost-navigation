{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#if svgPath}}
    {{frost-icon class="icon" icon=svgPath}}
        {{alias}}
    {{frost-icon class="transition-arrow" icon="launch/transition-arrow"}}
  {{else}}
        {{alias}}
  {{/if}}
{{/if}}