{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#if svgPath}}
    {{frost-icon class="icon" icon=svgPath}}
        {{alias}}
    {{frost-icon class="transition-arrow" icon="frost/chevron"}}
  {{else}}
        {{alias}}
  {{/if}}
{{/if}}
